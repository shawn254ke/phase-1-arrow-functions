// Define divide as a function expression
const divide = function(a, b) {
    return a / b;
  };
  
  // Define square as an arrow function
  const square = x => x * x;
  
  // Define add as an arrow function
  const add = (a, b) => a + b;
  
  // Export functions for testing if needed
  module.exports = { divide, square, add };
  